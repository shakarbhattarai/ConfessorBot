class SendMessage:
    def __init__(self,message,sendToName):
        self.message=self.message
        self.sendToName=sendToName


    def send(self):
        #Add timestamp value to when the message is to be sent
        